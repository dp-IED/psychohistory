# Vault

Parent creates `graph-vault/` on the first reflection write (entities, threads, concepts). This branch does not ship a corpus; match whatever layout Parent starts.

On due-today, workers read vault facts only if that directory already exists. On reflection after `Y`, Parent writes or updates an entity, thread, or concept when the grade earned a durable fact.
