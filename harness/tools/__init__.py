"""Pluggable harness tools (retrieval, synthesis).

Import concrete factories directly, e.g. `from harness.tools.real_toolset import build_real_toolset`,
to avoid import cycles with `harness.agent_loop`.
"""
