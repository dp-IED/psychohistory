---
description: Psychohistory memory distillate — concise defaults, sequencing, and agent-maintained preferences
alwaysApply: true
last_reviewed: 2026-08-17
---

# Project memory (concise)

Purpose: skills/subagent training bed for temporally clean forecasting. Canonical detail lives in repo docs.

## Update protocol

1) Durable preference changed → update "User preferences" (YYYY-MM-DD).
2) Work deferred/on hold → update "Parked / planned later" with trigger.
3) Program fact/sequence changed → update sections below + bump `last_reviewed`.
4) Keep bullets short; dedupe; move narrative to canonical docs.

## Canonical docs

- `README.md` / `project.md` (what this repo is)
- `next_steps.md` (current sequence)
- `docs/polymarket_agentic_harness.md` (market frames / PIT labels)
- `docs/history/research-track.md` (deleted research program; do not treat as current)

## Non-negotiables

- No post-`t` leakage; replayable history by cutoff.
- Portable skills/subagents must not import hermes or `harness.orchestrator`.
- `harness/orchestrator.py` is a **temporary** runner to replace.

## Strategic defaults

- Gradients before grand design: thin correct loop first, then scale.
- Measurement over ontology expansion.
- Vault + PIT + probes are the training surface; markets are labels and coverage, not the product.

## Sequencing (compressed)

Next: skill/subagent interface → replace hermes v1 → eval on existing PIT probes. Tournament watcher rewrite later. Do not resume France/GNN/builder.

## Engineering checks

- Default pytest must pass without hermes on PATH.
- Re-run PIT admissibility tests when vault layout or `vault_pit.py` changes.

---

## User preferences (agent-maintained)

- 2026-04-30: For novel architecture work, prioritize fast generation↔verification loops (harness-first) over broad implementation progress.
- 2026-04-30: Treat locked-decisions tables as operational memory; decisions are not reliably "locked" unless recorded there.
- 2026-05-03: For `R(q, h_t)`, avoid a fixed global schema; use a minimal invariant envelope for audit/logging while optimizing the query-conditioned representation process itself.
- 2026-05-03: LLM-in-loop seed set locked to 30 resolved Polymarket-only questions: 10 politics/institutional power, 10 economics/macro-policy, 10 culture/entertainment. Culture belief extraction is expected weaker; 10/domain tests anti-collapse/scaffold diversity, not powered calibration.
- 2026-08-17: Pivot to harness-only training bed; delete research track from working tree; v1 hermes runner is temporary; v2 cognitive pipeline deleted.

## Parked / planned later (agent-maintained)

- France/GNN/warehouse/builder/WM · parked permanently unless un-parked · last full tree: `b8ac2d66` / `origin/main` `b50ae186`.
- GNN-agent worktree · parked · path `conductor/workspaces/psychohistory-v2/gnn-agent`.
- Tournament watcher · rewrite from scratch; do not restore v2.
- Predictive-coding centrality trajectory objective · parked (research-era).
- Wikipedia point-in-time ingestion expansion · parked (research-era).
