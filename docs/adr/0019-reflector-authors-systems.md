# Overlay is not markdown-only; the reflector authors the systems it needs

The plugin is instructions **plus** whatever deterministic machinery a grade earned. After resolution day the reflector grades **the claim series and the overlay system that produced it** (cards, skills, scripts, models, `exp/` tools). It then writes analog cards and skills **and**, when prose cannot keep the mechanism honest, code: `scripts/`, tests, small models, a GNN, a class-local index. Tools that transferred stay; tools that were load-bearing and wrong are rewritten or culled on the same tick as a false analog card. It chooses the tool because it saw the miss. The human does not pre-install a cliodynamics stack.

New agents, skills, scripts, and organisation still start on `exp/<slug>` (ADR 0016). Tiny live wording may stay on `harness-only`.

Do not restore the France/warehouse GNN tree or `graph-vault/`. A new simulator is overlay-grown, not a checkout of `b8ac2d66`.

Supersedes any reading of ADR 0007 that treated simulators as a distant markdown-only epoch.
