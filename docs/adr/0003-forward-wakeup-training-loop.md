# Training loop is live wakeups, scored in the future

Plugin epochs come from claims written now, due at `Y`, resumed by the host’s `/loop` or `/automate`, then reflected. Fan-out is many specific real-world claims. This loop does not use historical cutoffs or gold Brier.

A 2023 replay cannot grade an LLM. A claim made today, scored when `Y` arrives, can.
