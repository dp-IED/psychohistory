# Ledger at repo root; K is a knob; host jobs are deferred

The schedule book is `ledger.md` at the repository root, not inside a vault corpus or the plugin overlay. Vault gets promoted facts on reflection; the ledger stays the Parent’s one read.

`K` (new problems per discovery tick) lives on the ledger; start at 1. Due-today vs discovery are two host jobs (`/loop` or `/automate`); do not wire them until the overlay and ledger exist (they do; ticker still deferred).
